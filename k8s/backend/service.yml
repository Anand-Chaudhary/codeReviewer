apiVersion: v1
kind: Service
metadata:
  name: codeReviewer-backend-service
spec:
  selector:
    app.kubernetes.io/name: codeReviewer-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
    
