apiVersion: v1
kind: Service
metadata:
  name: codeReviewer-frontend-service
spec:
  selector:
    app.kubernetes.io/name: codeReviewer-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5173
    
