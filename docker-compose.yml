services:
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    networks:
      - code-reviewer
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    networks:
      - code-reviewer

networks:
  code-reviewer:
    driver: bridge